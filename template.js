const Discord = require('discord.js');
module.exports = {
    name: 'error',
    description: 'Sends an error message',
    command: '`[No Command]`',
    async execute(message, args, client) {
    }
}